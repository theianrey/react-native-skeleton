/**
 * * This file will combine all created custom reducers
 * * and will store them to a single constant variable
 * @var RootReducer
 */

import {combineReducers} from 'redux';
// * import your reducers below

// * combines all reducers
const RootReducer = combineReducers({
  /**
   * * create a key/value pair for your imported reducer
   * @sample
   *    key: reducer
   */
});

// * export your root reducer
export default RootReducer;
